@page
@model EmployeeSite.Pages.EmployeesModel
@{
    ViewData["Title"] = "Employees";
}

<h1>Employee Directory</h1>

@foreach(var employee in Model.Employees)
{
    <div>
        <p>@employee.Name is at <a href="mailto:@employee.Email">@employee.Email</a></p>
    </div>
}
